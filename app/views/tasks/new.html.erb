<h1>Nouvelle tâche</h1>

<%= form_with(model:@task, local: true) do |f| %>
<%= f.label :title %><br />
<%= f.text_field :title %>
<%= f.label :details %><br />
<%= f.text_area :details %>
<%= f.submit 'Submit' %>
<% end %>

<%= link_to 'back to list', tasks_path %>
